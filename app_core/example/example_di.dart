import 'package:get_it/get_it.dart';

/// Example-only service locator for `app_core/example/*`.
///
/// Consumer apps must register dependencies in their own DI module
/// (for example `lib/config/di/` in the host app).
final getIt = GetIt.instance;
