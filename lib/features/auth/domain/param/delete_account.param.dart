library;

// Path/query/body inputs for landing APIs.
// Template: VS Code snippet `par` / `parj` (prefix `par`, `parj`).

class DeleteAccountParams {
  const DeleteAccountParams({
    required this.userId,
  });

  final String userId;
}
