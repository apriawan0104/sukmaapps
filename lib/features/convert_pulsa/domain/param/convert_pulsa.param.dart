library;

// Path/query/body inputs untuk API convert pulsa.
// Template: VS Code snippet `par` / `parj` (prefix `par`, `parj`).

class GetConvertPulsaItemParams {
  const GetConvertPulsaItemParams({
    required this.id,
  });

  final int id;
}
